import styled from "styled-components";

const Button = styled.button`
  background: #ccd;
  color: #333;
  border: 0;
  border-radius: 5px;
  padding: 0.5rem 1rem;
  cursor: pointer;
`;

export default Button;
